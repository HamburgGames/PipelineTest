name: Tag

on:
  workflow_dispatch: {}

jobs:
  activate:
    name: Create new Tag
    runs-on: ubuntu-latest
    steps:
      - name: Create an incremental tag
        uses: aurestic/incremental-tag@1.0.8
