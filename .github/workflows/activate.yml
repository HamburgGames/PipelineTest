name: Activate
on:
  workflow_dispatch: {}
jobs:
  activate:
    name: Activates the current Unity license
    runs-on: ubuntu-latest
    steps:
      - name: Unity - Activate
        uses: game-ci/unity-activate@v2.0.0
